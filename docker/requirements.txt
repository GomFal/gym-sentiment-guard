# Serving API dependencies
# Used by Dockerfile for production container
# Contains only the minimal dependencies needed for model serving

# Core ML (pinned to match training environment)
pandas>=2.1.0
numpy>=1.26.0
scikit-learn==1.7.2  # Must match model training version
joblib>=1.3.0

# API Framework
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0

# Configuration
pyyaml>=6.0.1
